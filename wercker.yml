box: ubuntu

build:
  steps:
    - script:
      code: | 
        echo "do something"
    - internal/docker-push:
      repository: nigeldeakin/docker-push-no-registry-repository
      registry: https://docker.io
      username: $REPO_USERNAME
      password: $REPO_PASSWORD
