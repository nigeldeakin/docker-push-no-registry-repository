box: ubuntu

build:
  steps:
    - script:
      code: | 
        echo "do something"
    - internal/docker-push:
      repository: https://wcr.io/nigeldeakin/docker-push-no-registry-repository
      username: $REPO_USERNAME
      password: $REPO_PASSWORD
